// import Konva from 'konva';
import { stage, layer, q_1, a_1 } from './initialization';
import { arrowControl } from './control';

// Initialize the canvas
arrowControl(stage,layer,q_1,a_1);
layer.draw();